services:
  db:
    image: pgvector/pgvector:pg16
    container_name: rag-system-db
    environment:
      POSTGRES_USER: rag
      POSTGRES_PASSWORD: rag
      POSTGRES_DB: rag
    ports:
      - "5433:5432"
    volumes:
      - rag_pg_data:/var/lib/postgresql/data

volumes:
  rag_pg_data:
